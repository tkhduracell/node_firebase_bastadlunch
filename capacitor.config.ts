import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Båstad Lunch',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
